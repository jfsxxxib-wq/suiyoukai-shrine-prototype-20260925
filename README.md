# 組み合わせ神社：操作できる試作

2026-09-24に、試作の仕様として確認済みです。公開ポータルへの組み込みは別工程です。

`working.html` を開くと、参加者名簿、ペア・団体、トーナメント、スイス方式、結果の保存と転送を試せます。初期データは空です。

点数の新規入力は選択式で－10～30、段は1～9、級は1～30です。以前に作った転送データの点数は、範囲外でも失わないよう読み取り時の制限を変更していません。

この試作は端末内に保存します。正式台帳、スタンプ、公式対戦結果、公開ポータルには接続していません。保存する結果は最新5件です。

計算部分の確認は次のコマンドで行えます。

```sh
node --test-isolation=none --test shrine-core.test.cjs
```

このフォルダの `index.html` と画像は過去の静止見本です。動く試作は `working.html` です。
